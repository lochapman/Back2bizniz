  function myFunction() {
      let myArray = newArray(1);
      let myArray2 = ['richard','rasmus', 'Tony','Peter', 'Jake'];
      myArray2[myArray.length - 1];
      myArray2[99] = 'Richter';
      return myArray2;
  }
//write a function that takes a string as an argument and turns it into an array of uppercase characters
function oneThing (str) {
  const thingOne = str.toUpperCase;
  let thingTwo = thingOne.split ;
  console.log ("upperstring", thingOne);
}

function twoThing (guildedheights) {

}

//you can pass strings as parameters instead, making it easier
//